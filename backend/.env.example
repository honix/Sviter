# Backend .env - Used for local development (make backend)
# For Docker container tests, see root .env instead
#
# Copy this file to .env and fill in your values

# Path to your wiki git repository (required)
# Default wiki is included as submodule (git submodule update --init)
WIKI_REPO_PATH=../Sviter-wiki

# OpenRouter API key (required if using OpenRouter adapter)
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Claude Agent SDK Configuration
# If ANTHROPIC_API_KEY is set, it uses API (pay-as-you-go billing)
# If not set, uses Claude subscription login (claude login)
# Get API key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# LLM Configuration
# Model to use (default: claude-sonnet-4-5)
LLM_MODEL=claude-sonnet-4-5

# Provider: "claude" (Claude SDK) or "openrouter" (default: claude)
LLM_PROVIDER=claude

# # LLM Configuration openrouter grok-4.1-fast
# LLM_MODEL=x-ai/grok-4.1-fast
# LLM_PROVIDER=openrouter