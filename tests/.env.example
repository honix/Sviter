# Test environment variables
# Copy to .env and fill in your values
#
# docker-compose reads this file automatically when run from tests/ folder

# API key for Docker-contained Claude Agent SDK
# Required for AI chat tests in containers (subscription login doesn't work in Docker)
# Get your API key from: https://console.anthropic.com/settings/keys
# Note: API usage is billed separately from Claude Pro/Max subscription
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
